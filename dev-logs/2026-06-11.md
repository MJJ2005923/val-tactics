# 2026-06-11 开发日志

## 进度总览

本日完成 44 个提交，覆盖 15+ 模块。dev 分支已提交，未部署公网。

---

## 一、已完成

### 安全加固（6项）
- Supabase 密钥环境变量化（从源码移除硬编码）
- AI 防提示注入（6条安全守则）
- IP 级别限流（30次/分钟 + 500次/小时，Cloudflare KV）
- 用量存储从内存 Map 改为 KV（防冷启动清零）
- 用户自备 API Key XOR+Base64 加密存储（绑定用户 ID）
- Google API Key 从 URL 参数改为 Header 传递

### 内容安全
- 敏感词库：5 类 180+ 词（政治/色情/暴力/赌博诈骗/仇恨歧视）
- 双审核：词库快速匹配 + DeepSeek AI 深度审核
- /api/content-filter 端点

### AI 收费方案
- 简化为 ¥30/月 统一套餐：免费/月付 ¥30/季付 ¥75/年付 ¥288 + 自备Key ¥19.9
- 日次数按模型分拆：Fast 20 / Balanced 10 / Reasoning 3 / Deep 2
- 老客户迁移方案（进阶/专业自动折算延长）
- 前端 AIPage/AISettings 同步更新

### 社区系统
- MVP：战术广场（搜索/排序/地图过滤）+ 详情 + 评论
- V2：点赞 + 关注 + 通知铃铛（30秒轮询 + 未读红点）+ 论坛（4分类/发帖/详情）+ 个人主页
- 全屏页面：论坛 + 点位 + 帖子详情 + 个人主页
- 删除权限：仅创作者 + 管理员（sessionStorage admin-key 判断）

### 战术发布增强
- 预览图：卡片 80px 缩略图 + 详情页全宽大图
- 多图上传：点位图 + 效果图组，不限数量
- 点位数量字段 + 地图选择器
- 卡片 6 列紧凑布局（36 个/屏，桌面 6 列/平板 4 列/手机 3 列/小屏 2 列）
- 收藏数展示

### 技能点位 Lineups
- 点位频道（地图/特工筛选 + 难度星级）
- 四图上传（站位/瞄点/释放/效果）+ 四宫格预览 + 图片放大
- Supabase Storage 集成：前端 WebP 压缩 ≤300KB → 上传 → 公开 URL
- 一键初始化 Storage bucket（管理面板按钮）
- 内嵌地图坐标选点：站位(青点) + 落点(粉点) + 虚线连线
- 删除自动清理 Storage 图片
- ProfilePage 点位 Tab

### 移动端
- MobileLayout 独立布局：底部 4 Tab（地图/英雄/时间轴/社区）+ 英雄抽屉 + 时间轴抽屉
- 顶部工具栏：地图选择 + 攻守切换 + 通知 + 登录
- 底部快捷栏：模板 + 保存 + 工具条
- MapCanvas 触控：单指拖拽平移 + 双指捏合缩放 + 移动端默认 1.5x
- viewport 恢复双指缩放
- 横屏 1024px 适配（画布最大化/导航精简/时间轴 120px）
- 竖屏 768px portrait 适配
- 全部 emoji 去除

### 个人主页升级
- 6 项统计：战术 / 点位帖子 / 获赞 / 被收藏 / 粉丝 / 关注
- 操作按钮：关注 + 赞账号 + 收藏（FavoriteProfileButton）
- 套餐条（仅自己可见）：显示"标准版 · 剩余 N 天"或"免费版 · 升级解锁"
- 账户设置：重置密码 + 退出登录
- 头像上传：Storage avatars bucket → 更换蒙层
- 编辑昵称和简介
- DEV 开发者徽章（profiles.is_admin）
- 导航栏用户名点击进入个人主页

### PWA
- manifest.json + Service Worker + 离线缓存 + 安装到桌面

### 基础设施
- GitHub 仓库 + 双 push（Gitee + GitHub，SSH 443 端口代理）
- CodeRabbit 自动代码审查
- Cline VS Code 插件（开发备用 Agent）
- 管理面板：用户统计 + 套餐分布 + 千人提醒 + Storage 初始化

---

## 二、未完成

| 事项 | 说明 |
|------|------|
| **部署公网** | dev 分支 44 个提交未上线，master 停留在社区 MVP |
| 多人实时协作 | rooms 表 + Realtime 广播 + 房间面板（第二梯队） |
| 触控细节 | 技能拖拽 touch 版 + freehand touch + 长按菜单（第三梯队） |
| 引擎商业化 | >1000 用户后启动 |

---

## 三、数据库变更（Supabase 需执行）

```sql
-- tactical_shares 扩展
ALTER TABLE public.tactical_shares ADD COLUMN IF NOT EXISTS preview_image TEXT;
ALTER TABLE public.tactical_shares ADD COLUMN IF NOT EXISTS lineup_images TEXT[] DEFAULT '{}';
ALTER TABLE public.tactical_shares ADD COLUMN IF NOT EXISTS effect_images TEXT[] DEFAULT '{}';
ALTER TABLE public.tactical_shares ADD COLUMN IF NOT EXISTS favorite_count INT DEFAULT 0;

-- 个人主页升级
CREATE TABLE IF NOT EXISTS public.profile_favorites (...);
ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS favorite_count INT DEFAULT 0;
ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS is_admin BOOLEAN DEFAULT false;

-- 按月模型分拆 RPC
get_profile_stats 函数 / toggle_like 修复 / increment_favorite_count / decrement_favorite_count
```

---

## 四、Supabase Storage Buckets

需创建 public bucket：`lineups`、`avatars`
（管理面板 → 初始化 Storage → 一键创建）

---

## 五、下一步

1. 部署公网（merge dev → master → wrangler deploy）
2. 多人实时协作
3. 触控细节
4. ICP 备案 + 营业执照（用户 >1000 时）
